name: nyc311
type: snowflake
options:
  # account / username / password are supplied at runtime via the
  # EVIDENCE_SOURCE__nyc311__* env vars in docker-compose.yml (from .env),
  # so no secrets live in this committed file.
  database: NYC311
  warehouse: COMPUTE_WH
  role: ACCOUNTADMIN
  schema: NYC_311_GOLD
  authenticator: snowflake
